const data = fetch("competeties.json")
                .then(response => response.json())
                .then(data => {
                    document.querySelector("#id").innerText = data.JSONid
                },


function showData(tab, subTab) {
for (let i = 0; i < len(data.tab.subTab); i++){
    <div class="c-card">
        <div class="c-header">
            data.tab.subTab.i.Key
        </div>
        <div class="c-body">
            <div>data.tab.subTab.i.Value</div>
        </div>
    </div>
}
}

function beroepsTaken(taak, laag){
    document.querySelector("#beroepstaken").innerText = data.beroepstaken.//classid
}              